# ============================================================================
# DATABASE TOOLS - ENVIRONMENT VARIABLES TEMPLATE
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your actual credentials
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================
# Get these from: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key-here

# Note: SUPABASE_KEY can be used as fallback if SUPABASE_SERVICE_KEY is not set
# SUPABASE_KEY=your-supabase-key-here

# ============================================================================
# USAGE
# ============================================================================
# Database tools use these credentials to:
# - Validate schema files across all engines
# - Generate SQL migrations from JSON schemas
# - Create/update tables in Supabase
# - Run database setup and validation
#
# Commands:
#   npm run db:validate        # Validate all schema files
#   npm run db:setup           # Create/update all tables
#   npm run db:setup -- --dry-run    # Preview SQL without executing
#
# ============================================================================

# ============================================================================
# SECURITY NOTES
# ============================================================================
# ⚠️ NEVER commit .env or any file with real credentials to git!
# ⚠️ Keep service role keys secure - they have full database access
# ⚠️ Rotate keys immediately if accidentally exposed
# ⚠️ Use different credentials for dev/staging/production environments
# ⚠️ Review .gitignore to ensure .env is excluded
