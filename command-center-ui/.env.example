# ============================================================================
# COMMAND CENTER UI - ENVIRONMENT VARIABLES TEMPLATE
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!
#
# Usage:
#   cp .env.example .env.local
#   # Edit .env.local with your actual credentials
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_KEY=your-supabase-anon-or-service-key-here

# ============================================================================
# AGENT API ENDPOINTS
# ============================================================================
# Agent 1: Prospecting Engine (default port: 3010)
NEXT_PUBLIC_PROSPECTING_API=http://localhost:3010

# Agent 2: Analysis Engine (default port: 3000)
NEXT_PUBLIC_ANALYSIS_API=http://localhost:3000

# Agent 3: Outreach Engine (default port: 3001)
NEXT_PUBLIC_OUTREACH_API=http://localhost:3001

# Agent 6: Pipeline Orchestrator (default port: 3020)
NEXT_PUBLIC_ORCHESTRATOR_API=http://localhost:3020

# Production endpoints (uncomment and configure for production)
# NEXT_PUBLIC_PROSPECTING_API=https://your-domain.com/api/prospecting
# NEXT_PUBLIC_ANALYSIS_API=https://your-domain.com/api/analysis
# NEXT_PUBLIC_OUTREACH_API=https://your-domain.com/api/outreach
# NEXT_PUBLIC_ORCHESTRATOR_API=https://your-domain.com/api/orchestrator

# ============================================================================
# OPTIONAL: ADDITIONAL CONFIGURATION
# ============================================================================
# Uncomment and configure as needed

# Node environment
# NODE_ENV=development

# App URL (for production)
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# Enable debug logging
# NEXT_PUBLIC_DEBUG=false

# ============================================================================
# SECURITY NOTES
# ============================================================================
# ⚠️ NEVER commit .env.local or any file with real credentials to git!
# ⚠️ Always use NEXT_PUBLIC_ prefix for variables exposed to browser
# ⚠️ Keep service role keys secure - never expose in browser code
# ⚠️ Rotate keys immediately if accidentally exposed
# ⚠️ Use different credentials for dev/staging/production environments
# ⚠️ Review .gitignore to ensure .env.local is excluded
