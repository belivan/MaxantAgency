{
  "name": "maksant-agency",
  "version": "1.0.0",
  "description": "Complete lead generation pipeline - unified interface",
  "private": true,
  "scripts": {
    "start": "concurrently --names \"EMAIL,UI\" --prefix-colors \"cyan,magenta\" \"npm run start:email\" \"npm run start:ui\"",
    "start:email": "cd email-composer && npm start",
    "start:ui": "cd command-center-ui && npm run dev",
    "dev": "npm start",
    "install:all": "npm install && cd email-composer && npm install && cd ../command-center-ui && npm install && cd ../client-orchestrator && npm install && cd ../website-audit-tool && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "Maksant",
  "license": "MIT"
}
